# Claude (Anthropic) - used for mediator, reflections, and lib/claudeAgent
ANTHROPIC_API_KEY=
# Optional alias (either works)
# CLAUDE_API_KEY=

# --- Voice: OpenAI diarization + speaker clustering (recommended) ---
OPENAI_API_KEY=
# Optional: real speaker IDs (ECAPA-TDNN). Run: npm run embedder (or see README), then uncomment:
# SPEAKER_EMBEDDER_URL=http://localhost:5000/embed
SPEAKER_EMBEDDER_URL=
SPEAKER_MATCH_THRESHOLD=0.72
CONVO_GAP_MS=600000

# --- Voice: Google + Azure (optional, for enrolled-speaker identification only) ---
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_CLOUD_PROJECT=
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=westus
